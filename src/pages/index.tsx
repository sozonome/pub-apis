import { Box } from "@chakra-ui/react";

import SearchContainer from "../components/form/SearchContainer";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <SearchContainer />
    </Box>
  );
};

export default Home;
